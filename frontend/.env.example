# Backend API Configuration
VITE_API_BASE_URL=http://localhost:5000

# Admin Panel Authentication
# Must match the ADMIN_PASSWORD environment variable set on the backend server.
# Change this in production — never commit the real value to source control.
VITE_ADMIN_TOKEN=admin123

# Development
VITE_DEBUG=true
